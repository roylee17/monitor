# Devnet-specific values for ics-validator
# This file contains common settings for devnet deployments

# Use the standard test mnemonic
keys:
  type: "mnemonic"
  mnemonic: "guard cream sadness conduct invite crumble clock pudding hole grit liar hotel maid produce squeeze return argue turtle know drive eight casino maze host"

# Devnet mode enabled
devnet:
  enabled: true
  kindCluster: true
  useDockerInternal: true

# Use NodePort for Kind clusters
service:
  type: NodePort

# Don't use persistent storage for devnet
storage:
  validator:
    size: ""
  monitor:
    size: ""

# Lower resource requirements for devnet
resources:
  validator:
    limits:
      cpu: 200m
      memory: 256Mi
    requests:
      cpu: 50m
      memory: 128Mi
  monitor:
    limits:
      cpu: 100m
      memory: 128Mi
    requests:
      cpu: 25m
      memory: 64Mi

# Disable pod security restrictions for devnet
podSecurityContext:
  fsGroup: 1000
  runAsUser: 1000
  runAsNonRoot: false

# Allow privilege escalation for init containers
securityContext:
  allowPrivilegeEscalation: true
  readOnlyRootFilesystem: false